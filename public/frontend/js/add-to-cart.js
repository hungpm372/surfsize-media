$(document).ready((function () { $(document).on("click", ".add-to-cart", (function (t) { t.preventDefault(); var n = $(this), a = $('input[name="product-quatity"]').val() ? $('input[name="product-quatity"]').val() : 1; $.ajax({ url: n.data("url"), data: { _token: $('meta[name="csrf-token"]').attr("content"), id: n.data("id"), price: n.data("price"), quantity: a }, method: "POST", dataType: "json", success: function (t) { if (1 == t.status) { Swal.fire({ icon: "success", title: "Hoàn tất!", text: "Thêm sản phẩm thành công", confirmButtonText: "Đóng" }); var n = parseInt($(".left-info .product-quantity").text()) + parseInt(a); $(".left-info .product-quantity").text(n) } else Swal.fire({ icon: "error", title: "Lỗi!", text: "Sản phẩm đã đạt số lượng tối đa!", confirmButtonText: "Đóng" }) }, error: function (t, n, a) { 401 == t.status && (window.location.href = "/login") } }) })) }));